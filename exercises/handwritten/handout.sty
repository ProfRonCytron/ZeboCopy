%
% HANDOUT.STY
%
\usepackage[left=0.7in,right=0.7in,top=0.7in,bottom=0.7in]{geometry}
\usepackage{graphicx}
\usepackage{comment}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage[noend]{algpseudocode} % for pseudocode
\usepackage{algorithm}
\usepackage[bookmarks=false]{hyperref} % for URLs

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% SET COURSE NAME HERE
\newcommand{\CourseName}{CSE 468T Intro to Quantum Computing}
\newcommand{\Semester}{Spring 2025}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% Use wide inter-line spacing for readability
\renewcommand{\baselinestretch}{1.1}

% preferred pseudocode style
\algrenewcommand{\algorithmicprocedure}{}
\algrenewcommand{\algorithmicthen}{}
\algdef{SE}[DOWHILE]{Do}{doWhile}{\algorithmicdo}[1]{\algorithmicwhile\ #1}%
\newcommand{\ForRange}[3]{\For{#1 \textbf{in} #2 \ \ldots \ #3}}

% common missing math
\newcommand{\union}{\cup}
\newcommand{\isect}{\cap}
\newcommand{\ceil}[1]{\ensuremath  \left\lceil{#1}\right\rceil}
\newcommand{\floor}[1]{\ensuremath \left\lfloor{#1}\right\rfloor}


% Create a box with course name in upper left, arg 2 in upper right
% (e.g. ``Lab 3''), arg 1 in large type in the center, arg 3 in
% the lower left (date assigned), and arg 4 in the lower right (date
% due).  Use this as the header for assignments.
%
\newcommand{\assignment}[2]{
   \pagestyle{plain}
   \noindent
   \begin{center}
   \framebox{
      \vbox{
    \hbox to \textwidth { \textbf{\CourseName{}} \hfill \Semester{} } 
       \vspace{4mm}
       \hbox to \textwidth {\Large \hfill #1 \hfill}
       \vspace{1mm}
      }
   }
   \end{center}
   \vspace*{4mm}
}

\def\Points#1{\mbox{(#1~points)}}
% Create a box with course name in upper left, arg 2 in upper right
% (e.g. a date) and arg 1 in large type in the center.  Use this
% as the header for course handouts.
%
\newcommand{\handout}[2]{
   \pagestyle{plain}
   \noindent
   \begin{center}
   \framebox{
      \vbox{
    \hbox to \textwidth { \textbf{\CourseName{}} \hfill #2 }
       \vspace{4mm}
       \hbox to \textwidth {\Large \hfill #1 \hfill}
       \vspace{2mm}
       \hbox to \textwidth { }
      }
   }
   \end{center}
   \vspace*{4mm}
}
\def\Ans#1{{\color{blue}\mbox{Answer: }#1}}
\def\DefaultSpace{1in}
\newcommand{\LeaveSpace}[1][\DefaultSpace]{%
\vskip #1 plus 1fil\relax\hbox to 0pt{\hss} %
}
\def\Obox#1{\Ovalbox{\hbox to 1ex{\vrule width 0pt height 1ex\hss #1\hss}}}
\def\TFMarked#1#2{\ \stackbox[l][m]{\Obox{#1}~\textbf{true}\\\Obox{#2}~\textbf{false}}}
\def\TF{\TFMarked{\relax}{\relax}}
\def\TFH{\Obox{\relax}~\textbf{true}~~~\Obox{\relax}~\textbf{false}}